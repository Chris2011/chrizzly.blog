<!DOCTYPE HTML>

<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>{{ site_title }}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="{{ meta.description|striptags }}" />

        <!-- Facebook and Twitter integration -->
        <meta property="og:title" content="{{ site_title }}"/>
        <meta property="og:image" content="{{ meta.image }}"/>
        <meta property="og:url" content="{{ base_url }}"/>
        <meta property="og:site_name" content="{{ site_title }}"/>
        <meta property="og:description" content="{{ meta.description|striptags }}"/>
        <meta name="twitter:title" content="{{ site_title }}" />
        <meta name="twitter:image" content="{{ meta.image }}" />
        <meta name="twitter:url" content="{{ base_url }}" />
        <meta name="twitter:card" content="summary_large_image" />

        <link href="https://fonts.googleapis.com/css?family=Work+Sans:300,400,500,700,800" rel="stylesheet">

        <!-- Animate.css -->
{#        <link rel="stylesheet" href="{{ theme_url }}/css/animate.css">#}
        <!-- Icomoon Icon Fonts-->
{#        <link rel="stylesheet" href="{{ theme_url }}/css/icomoon.css">#}

        <!-- Theme style  -->
        <link rel="stylesheet" href="{{ theme_url }}/css/style.css">
    </head>

    <body>
        <div id="page">
            <div id="fh5co-aside">
                <div class="overlay"></div>
                <div class="about-us">
                    <div class="avatar chris"></div>
                    <div>
                        <h3>Christian Lenz (<a href="" class="external-links">@Chrizzly42</a>)</h3>
                        <div><a class="external-links" href="mailto:chris@puls-webagentur.de">chris@puls-webagentur.de</a></div>
                    </div>
                </div>
                <div class="featured">
                    <h2>{{ meta.bio }}</h2>
                </div>
            </div>
            <div id="fh5co-main-content">
                <div class="fh5co-post">
                    {% for page in pages|sort_by("time")|reverse %}
                        {% if page.meta.template == "single" %}
                            <div class="fh5co-entry padding">
                                <img src="{{ page.meta.thumbnail }}" alt="post thumbnail" />
                                <div>
                                    <span class="fh5co-post-date">{{ page.date }}</span>
                                    <h2><a href="{{ page.url }}">{{ page.title }}</a></h2>
                                    <p>{{ page.excerpt }}</p>
                                </div>
                            </div>
                        {% endif %}
                    {% endfor %}
                    <footer>
                        <div>
                            &copy; 2017. All Rights Reserved.
                        </div>
                    </footer>
                </div>
            </div>
        </div>

        <div class="gototop js-top">
            <a href="#" class="js-gotop"><i class="icon-arrow-up"></i></a>
        </div>

        <!-- Main -->
        <script src="{{ theme_url }}/js/main.js"></script>
    </body>
</html>